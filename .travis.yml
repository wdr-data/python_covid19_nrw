language: python

sudo: false

python:
  - "3.8"

env:
  global:
    - AWS_REGION=eu-central-1

cache: yarn

before_script:
  - nvm install 12
  - nvm use 12
  - yarn

skript: echo "no tests 😟"

deploy:
  - provider: script
    skip_cleanup: true
    script: STAGE=staging yarn serverless deploy
    on:
      branch: master
  - provider: script
    skip_cleanup: true
    script: STAGE=prod yarn serverless deploy
    on:
      branch: production
